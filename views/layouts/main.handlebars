<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Muse-sik</title>
        <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css"
        rel="stylesheet"
        integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi"
        crossorigin="anonymous"
        />
        <link
        rel="stylesheet"
        href="https://fonts.googleapis.com/css?family=Tangerine"
        />
        <link rel="stylesheet" type="text/css" href="./css/style.css" />
    </head>
    <body>
        <header >
            <div class="d-flex justify-content-between">
                <img src="./img/muse-sik.png" alt="muse-sik logo">
            <!-- Conditionally render login or logout links -->
                {{#if logged_in}}
                <button id="btn-menu"><i class="fa-solid fa-bars"></i></button>
            </div>     
            <nav>
                <ul class="nav-bar d-flex justify-content-evenly align-items-center">
                    <li><a href="">Favorite</a></li>
                    <li><a href="">Search</a></li>
                    <li><button id="btn-playlist">Playlist</button></li>
                    <li><button id="logout">Log Out</button></li>
                </ul>
            </nav>
                {{else}}
            <p><a id="btn-login" href="/login">Log In</a></p>
                {{/if}}
        </header>
        <main>
            <!-- Render the sub layout -->
            {{{body}}}
        </main>
        <footer class="sticky-bottom">
            <p>&copy Khanh Bui, Brandon Rodriguez, and Faiyaz Alam 2022</p>
        </footer>
    </body>
    <script
    src="https://kit.fontawesome.com/30f6588d91.js"
    crossorigin="anonymous"
    ></script>
    {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</html>